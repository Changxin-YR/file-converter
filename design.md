# 产品与技术设计

## 产品目标

本地单机文件格式转换工具,聚焦**文档格式转换**、**图片格式转换**、**视频与音轨**和 **ZIP/时区工具**，在设备本地完成全部处理。应用不联网、无后端,数据不出设备,覆盖 phone / tablet / 2in1。

## 用户与场景

- 主要用户：需跨平台搬运文档/图片/视频格式的普通用户；重视隐私、拒绝上传文件的用户；需离线使用的用户
- 核心场景：文档打不开或需改格式、图片格式不兼容或需调整尺寸、视频容器不兼容
- 关键成功指标：支持格式覆盖度、转换成功率、转换速度与稳定性、离线可用性

## 设计约束（红线）

| 编号 | 约束 | 说明 |
| --- | --- | --- |
| R1 | 单机离线 | 不申请网络权限,无后端能力 |
| R2 | 权限最小化 | 优先系统文件选择器临时授权 |
| R3 | 数据不出设备 | 不收集、不上传、不存储用户文件 |

## 功能模块定义

| 优先级 | 模块 | 说明 |
| --- | --- | --- |
| 主 | 图片格式转换 | PNG / JPG / JPEG / WEBP / BMP 互转,尺寸调整 |
| 主 | 文档格式转换 | TXT / CSV / JSON / XML / HTML / MD 互转,编码转换 |
| 辅助 | 视频与音轨 | H.264/AAC MOV 无损重封装为 MP4；MP4 内 AAC 音轨无损重封装为 M4A |
| 辅助 | 档案与时间 | ZIP 压缩/解压与时区换算 |
| 辅助 | PDF 工具 | JPG/文本/HTML → PDF；项目自产未压缩文本 PDF → DOC/MD |
| 预留 | Office / 复杂 PDF | 🧩 离线三方引擎扩展点 |

## 页面信息架构

| 页面 | 职责 | 主要状态 |
| --- | --- | --- |
| Index | Tab 导航 + 功能卡片入口 | 卡片区 |
| DocumentConvertPage | 文本类文档互转 | 空态 / 转换中 / 成功 / 失败 |
| ImageConvertPage | 图片格式互转与尺寸调整 | 空态 / 转换中 / 成功 / 失败 |
| AboutPage | 版本与隐私声明 | 静态 |

## 核心交互

1. 选择文件 → 选择目标格式 → 配置参数 → 开始转换 → 展示结果/保存位置
2. 文档目标格式按全部已选源文件的共同可转换关系动态过滤；源编码支持 UTF-8、GBK、UTF-16LE，输出统一 UTF-8
3. 图片从系统文件选择器选择并限制为可解码后缀，不依赖相册是否已完成媒体索引；输出仍经系统授权保存到相册
4. 加载、空、错误、禁用和完成状态应有明确反馈
5. 未验证能力不出现在当前发布包的入口、格式选择、路由或能力说明中
6. 批量转换支持进度展示与中途取消

## 视觉系统

- 视觉基准：用户提供的八屏蓝白工具应用参考图，独立切片保存在 `docs/qa/reference-ui/`。
- 风格：明亮蓝白、本地工具质感；首页以品牌标题和格式文件主视觉建立首屏识别，其余页面保持紧凑任务流。
- 颜色：`ThemeTokens` 集中管理主蓝、浅蓝背景、中性文字、边框、成功、错误与禁用色，页面不得散落硬编码主题色。
- 字体层级：页面标题、区块标题、正文和辅助说明四级；紧凑控件不使用大标题字号。
- 组件：文件选择面板、格式胶囊、主操作按钮、分段控件和结果反馈统一复用共享组件；图标与插画使用项目内离线资源。
- 状态：仅发布已验证能力；不可执行的当前输入或设备条件使用明确错误反馈，不将未发布能力显示为占位功能。
- 页面背景：使用共享浅色背景和受约束内容宽度；独立页面不嵌套装饰卡片，宽屏保持内容居中且不被自动分栏压缩。
- 首页新版基准：32vp 品牌标题、15vp 副标题、设备验证后的 156vp 主插画、phone 双列/tablet 三列/2in1 四列功能卡和 72vp 双标签底栏。
- 关于页新版基准：128vp 应用图标、28vp 应用名、17vp 离线声明、四条独立图标隐私说明和可见版本信息。
- 两页参考原图独立保存在 `docs/qa/reference-ui-v2/`，设备运行截图不得覆盖设计原图。
- 应用图标以用户提供的蓝色环形箭头与文件主体为唯一视觉源；桌面、窗口和关于页使用同一份 1024x1024 RGBA PNG，禁止各入口出现不同版本。
- 当前用户可见应用名称为“文件格式盒”；名称由 AppScope 和 entry 的 `app_name` 资源统一提供，页面不得硬编码旧名称。

## 多设备适配

| 断点 | 宽度 | 形态 | 卡片列数 |
| --- | --- | --- | --- |
| SM | 320vp | phone 竖屏 | 2 |
| MD | 600vp | phone 横屏 / 小 tablet | 3 |
| LG | 840vp | tablet / 2in1 | 4 |

- 横竖屏策略：phone 竖屏优先,tablet / 2in1 支持横竖屏与窗口拖拽
- 安全区：`expandSafeArea` 处理系统安全区
- 独立工具页固定 `NavigationMode.Stack`，避免 tablet / 2in1 自动分栏将单页表单压缩到窄侧栏

## 状态与数据

- 核心状态：ConvertTask 状态机 PENDING / CONVERTING / SUCCESS / FAILED
- 页面级本地状态为主（`@State`）
- 持久化：无用户文件持久化需求；中间产物写入沙箱,结果由用户保存
- 系统能力：ImageKit / MediaKit / CoreFileKit；无网络依赖

## 无障碍

- 文字与背景保持足够对比,深色模式同步适配
- 触控目标尺寸清晰,状态不能只依赖颜色表达
- 关键图片、控件和错误反馈提供可理解语义

## 技术取舍与风险

| 决策 | 原因 | 风险 |
| --- | --- | --- |
| 引擎抽象层 | 隔离原生 Kit 与三方引擎,支撑格式扩展 | 抽象层开销极低 |
| 三方引擎离线约束 | 严禁网络下载；PDF/Office 有包体风险 | 分阶段评估 |
| 能力诚实标注 | 区分 ✅/🔶/🧩/❌,避免过度承诺 | 需真机复验 |
| AVSource + AVDemuxer + AVMuxer | AAC 音轨直接重封装,不依赖视频编码器 | 仅接受含 AAC 音轨的 MP4 |
| MOV 无损重封装 | 按时间戳交错复制 H.264/AAC sample 到 MP4，不启动编码器 | 其他轨道编码明确拒绝；通用转码仍需离线引擎 |
| 受限 PDF 文本提取器 | 对项目自产 PDF 提供可验证的反向转换 | 主动拒绝压缩、加密、扫描及复杂字体 PDF |

## 测试报告修复交互约束

- 文档批量转换先在应用沙箱生成临时结果，再通过一次系统保存对话框选择全部输出位置；取消保存时整批停止，不重复弹窗，临时结果必须清理。
- PDF 反向转换直接读取源文件字节，只支持项目自产未压缩文本 PDF；输入方式修正不扩大复杂 PDF 能力声明。
- 视频 Picker URI 必须先打开为文件句柄，再通过 fd 获取长度并交给转码或 Native 重封装能力。

## 第三轮审查后的主题与交互约束

- `resources/dark/element/color.json` 必须覆盖浅色主题的全部颜色名称，禁止依靠 `base` 资源回退；深色卡片、禁用控件、阴影和选中态均使用专用深色令牌。
- 浅色 `text_tertiary` 在 `surface_primary` 上保持 WCAG AA 文字对比度；进度条统一使用 `brand_primary`，不再使用兼容别名。
- 目标格式的“选中”视觉只可用于当前可交互的选项。发布包中的格式选择器仅提供已验证的目标格式。
- 视频 M4A 提取仅适用于 MP4 输入。用户选择 M4A 后，如已选文件含非 MP4，开始按钮禁用并给出明确原因；不把 Native 音轨提取的容器边界伪装为通用视频能力。
- 文件选择面板以 156vp 为基线最小高度而非固定高度，以允许系统大字体增加面板高度。图片与图标按钮提供读屏文本；首页内嵌和独立关于页复用同一隐私说明组件。
- ZIP 压缩暂存目录内的同名文件采用“名称 (2).扩展名”递增命名，避免在真正压缩前覆盖用户已选择的文件。
- PDF 文本流使用 UTF-16BE 代码单元编码；代理对字符不应将现有 `charCodeAt` 循环替换为单个 `codePointAt`，否则会破坏 PDF 所需的代理项序列。未随包嵌入可发布 CJK 字体时，中文显示仍取决于 PDF 查看器的 CIDFont 支持，需在发布目标设备复验。
